[project]
name = "an-illustrated-to-ai-agents"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ohmycloud", email = "ohmycloudy@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
an-illustrated-to-ai-agents = "an_illustrated_to_ai_agents:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
